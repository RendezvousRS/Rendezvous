# 存在的问题

>- scanf拼写拼错，拼成sanf
>- 这次问了ChatGPT,让它写一段算未知数量的数字的求和函数，它写了一段非常简单的函数。这里被完爆了
>- 然后说问题：
>- 主函数里double使用不当，这里就是第一版的问题“主函数调用分函数的格式问题”。正确写法应该是double bb = sum(); 然后对应的分函数里，是double sum(double xx , int y);这种
>- 在主函数之前未定义double sum(); double subs();…… 
