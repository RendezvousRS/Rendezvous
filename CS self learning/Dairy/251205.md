# 错题

同样一份包含汉字的代码文件，在CLion和VS 2026里可以正常执行，但在vscode里编译出的中文变成乱码。

问题的根源：编码格式本身

vscode默认UTF-8，在我把编译方式换成simplified chinese GNK之后，先是我代码中的汉字变成乱码，然后我把乱码汉字删掉，输入其他汉字。编译之后，能够正常输出汉字。

### GPT趣闻

询问了ChatGPT，它给的反馈是，出现乱码就应该把编译格式改成UTF-8。

而vscode本身就是默认UTF-8的（笑）
